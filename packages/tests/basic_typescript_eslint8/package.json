{
  "name": "basic-typescript-eslint8",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wireit",
    "lint": "echo skip lint script for integration test case",
    "test": "wireit",
    "update": "eslint src/ --format json | yarn run expect-violations update ./expected_violations.json"
  },
  "wireit": {
    "build": {
      "command": "wireit",
      "clean": "if-file-deleted",
      "files": [
        "src/**/*.ts",
        "tsconfig.json"
      ],
      "output": [
        "lib/**"
      ]
    },
    "test": {
      "command": "eslint --version | grep 'v8' && eslint src/ --format json | yarn run expect-violations test ./expected_violations.json",
      "dependencies": [
        "../../test-helpers/expect-violations:build"
      ],
      "env": {
        "SAFETY_WEB_LOG_PATH": "NONE"
      }
    }
  },
  "devDependencies": {
    "@safety-web/eslint-plugin": "workspace:^",
    "eslint": "^8.0.0",
    "expect-violations": "workspace:^",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.0.0",
    "wireit": "^0.14.12"
  }
}
